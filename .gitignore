# Local-only reference material: datasheets, photos, captures, mechanical
# scans. Keep large or non-redistributable files out of git.
/refs/

# KiCad (for when hardware/ lands)
*-backups/
*.kicad_prl
fp-info-cache
*.bak
*.kicad_pcb-bak
*.sch-bak
*.lck

# Editor local-history (nested git repo from VSCode Local History) -- never commit
.history/

# PlatformIO / build
.pio/
.vscode/
build/

# Python
__pycache__/
*.pyc
.venv/
venv/

# OS cruft
.DS_Store
Thumbs.db

# Claude Code local working state (permissions, per-machine logbooks) -- not team policy
.claude/
hardware/.kicad-mcp/
